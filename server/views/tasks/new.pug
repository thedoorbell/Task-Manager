extends ../layouts/application.pug

block header
  = t('views.tasks.new.title')

block content
  +formFor()(action=route('tasks') method='post')
    +input(task, 'name', errors)(type="text")
    +textarea(task, 'description', errors)(rows=3)
    +select(task, 'statusId', statuses, errors)
    +select(task, 'executorId', users, errors)
    +selectMultiple(task, 'labels', errors)
    +submit(task)(value=t('views.tasks.new.submit'))